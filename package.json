{
  "name": "integraled-lambda",
  "version": "1.0.0",
  "description": "Lambda backend for IntegralEd chatbot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.0.0",
    "node-fetch": "^2.6.1"
  }
}
